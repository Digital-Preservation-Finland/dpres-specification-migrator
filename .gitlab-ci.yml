---

variables:
  CI_EL9: "yes"
  CI_PYTHON_2_VENV: "yes"
  CI_SONAR: "yes"

include:
  - project: 'dpres/gitlab-ci-pipeline'
    ref: master
    file:
      - '/pipeline/common.yml'
      - '/pipeline/kppas.yml'
